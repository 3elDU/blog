<nav id="navbar" {% if glassNavbar %} class="glass" {% endif %}>
  <div class="title {% if glassNavbar %} glass-surface {% endif %}">
    <h2 id="navbar-website-title">petafloppa's blog</h2>
  </div>

  <ul class="links {% if glassNavbar %} glass-surface {% endif %}">
    <li>
      <a href="/" {% if page.url=="/" %} aria-current="page"
        class="current-page {% if glassNavbar %} glass-surface {% endif %}" {% endif %}>About</a>
    </li>
    <li>
      <a href="/articles" {% if page.url starts with '/articles' %} aria-current="page" class="current-page" {% endif
        %}>Articles</a>
    </li>

    <li>
      <a href="/toys" {% if page.url starts with '/toys' %} aria-current="page" class="current-page" {% endif %}>
        Toys</a>
    </li>
  </ul>
</nav>